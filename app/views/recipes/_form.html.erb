<%= form_for(@recipe) do |f| %>
<%#= form_for @recipe, url: {controller: "recipes", action: "create_from_search"}, method: "post" do |f| %>

Name: <%= params["name"] %></br>
Rating: <%= params["rating"] %>
<img src="<%= params["image"] %>"> 
<a href="<%= params["link"] %>">View recipe</a>
Ingredients:</br>
<% ingredients = params["ingredient_list"].split("&") %>
<% ingredients.each do |ingredient| %>
<%= ingredient %>
<% end %>


<%= hidden_field_tag "name", params["name"] %>
<%= hidden_field_tag "rating", params["rating"] %>
<%= hidden_field_tag "image", params["image"] %>
<%= hidden_field_tag "link", params["link"] %>
<%= hidden_field_tag "ingredient_list", params["ingredient_list"] %>

  <div class="cookbook-field">
    <%= f.label :cookbook %>
    <%=collection_select(:recipe, :cookbook_id, Cookbook.all, :id, :name, prompt: true)%>
  </div>

  <div class="note-field">
    <%= f.label :Note%><br>
    <%= f.text_field :note %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
  
<% end %>
